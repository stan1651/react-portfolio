import projectOnePic from "../pages/images/projectOnePic.png"

function ProjectContainer(title,imgUrl,about,siteUrl,repoUrl) {
    this.title = title;
    this.title..
}

const projectOne = new ProjectContainer (
    "ProjectOne",
    projectOnePic,
    "Description",
    "",
    ""
)

let portfolioData = [projectOne, projectTwo...];

export default portfolioData;